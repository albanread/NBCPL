LET START() BE $(
    WRITEF("Ultra-simple FPAIR test*N")

    LET f = FPAIRS(1)
    WRITEF("Allocated FPAIRS(1)*N")

    f!0 := FPAIR(5.0, 7.0)
    WRITEF("Assigned FPAIR(5.0, 7.0)*N")

    LET retrieved = f!0
    WRITEF("Retrieved: %Q*N", retrieved)

    WRITEF("Done*N")
$)
