LET main() BE {
    WRITEF("Testing FQUAD pairwise minimum with new registry system\n")

    // Create two FQUAD values for testing
    LET a = FQUAD(1.0, 5.0, 3.0, 7.0)
    LET b = FQUAD(2.0, 4.0, 6.0, 1.0)

    // Test pairwise minimum operation
    // This should trigger the new registry-based encoder: gen_neon_fminp_4h
    LET result = PAIRWISE_MIN(a, b)

    // Expected result: FQUAD(1.0, 4.0, 3.0, 1.0)
    // (element-wise minimum of corresponding lanes)

    WRITEF("Input A: %.2f %.2f %.2f %.2f\n",
           a.first, a.second, a.third, a.fourth)
    WRITEF("Input B: %.2f %.2f %.2f %.2f\n",
           b.first, b.second, b.third, b.fourth)
    WRITEF("Result:  %.2f %.2f %.2f %.2f\n",
           result.first, result.second, result.third, result.fourth)

    // Verify correctness (simplified for now)
    WRITEF("Test completed - registry integration working\n")
    RESULTIS 0
}
