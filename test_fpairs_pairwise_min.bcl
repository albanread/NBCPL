LET START() BE {
    WRITEF("Testing PAIRWISE_MIN with FPAIRS vectors*N")

    LET vec_a = FPAIRS 4
    LET vec_b = FPAIRS 4

    vec_a!0 := FPAIR(1.5, 2.5)
    vec_a!1 := FPAIR(8.0, 3.0)
    vec_a!2 := FPAIR(2.2, 7.7)
    vec_a!3 := FPAIR(9.1, 1.1)

    vec_b!0 := FPAIR(4.5, 1.0)
    vec_b!1 := FPAIR(6.0, 9.0)
    vec_b!2 := FPAIR(3.3, 6.6)
    vec_b!3 := FPAIR(8.8, 2.2)

    WRITEF("FPAIRS vectors initialized*N")

    LET result_min = PAIRWISE_MIN(vec_a, vec_b)

    WRITEF("PAIRWISE_MIN on FPAIRS completed successfully*N")
}
