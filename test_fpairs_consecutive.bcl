LET START() BE {
    WRITEF("Testing consecutive FPAIRS assignments*N")

    LET vec_a = FPAIRS 4
    LET vec_b = FPAIRS 4

    // Two consecutive assignments - this should crash
    vec_a!0 := FPAIR(2.0, 3.0)
    vec_b!0 := FPAIR(1.0, 4.0)

    WRITEF("Consecutive assignments completed*N")
}
