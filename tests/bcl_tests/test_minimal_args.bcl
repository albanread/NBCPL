LET START() BE
$(
   // Test minimal argument passing
   LET x = 42
   test_simple(x)

   // Test with literal
   test_simple(99)

   WRITEF("Done*N")
$)

LET test_simple(val) BE
$(
   WRITEF("Value: %N*N", val)
$)
