// test_simple_bitwise.bcl - Simple test for ~ operator

LET START() BE $(
    WRITES("Testing ~ operator*N")

    LET x = 5
    LET result = ~x

    WRITES("x = ")
    WRITEN(x)
    WRITES("*N")

    WRITES("~x = ")
    WRITEN(result)
    WRITES("*N")

    WRITES("Done*N")
$)
