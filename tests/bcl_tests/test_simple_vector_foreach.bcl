// Simpler vector FOREACH test to verify the temporary variable fix
LET start() BE {
    LET v = VEC 3
    v!0 := 10
    v!1 := 20
    v!2 := 30

    FOREACH x IN v DO {
        WRITEF("Value: %d\n", x)
    }
}
