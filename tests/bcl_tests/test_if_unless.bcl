LET START() BE
$(
  LET X = 5
  LET Y = 10

  IF X = 5 THEN WRITES("IF X = 5*N")
  IF X = 6 THEN WRITES("IF X = 6*N")
  IF Y < 20 THEN WRITES("IF Y < 20*N")
  IF Y > 20 THEN WRITES("IF Y > 20*N")
  IF X ~= 5 THEN WRITES("IF X ~= 5*N")
  IF X <= 5 THEN WRITES("IF X <= 5*N")
  IF Y >= 10 THEN WRITES("IF Y >= 10*N")

  UNLESS X = 5 THEN WRITES("UNLESS X = 5*N")
  UNLESS X = 6 THEN WRITES("UNLESS X = 6*N")
  UNLESS Y < 20 THEN WRITES("UNLESS Y < 20*N")
  UNLESS Y > 20 THEN WRITES("UNLESS Y > 20*N")
  UNLESS X ~= 5 THEN WRITES("UNLESS X ~= 5*N")
  UNLESS X <= 5 THEN WRITES("UNLESS X <= 5*N")
  UNLESS Y >= 10 THEN WRITES("UNLESS Y >= 10*N")
$)
