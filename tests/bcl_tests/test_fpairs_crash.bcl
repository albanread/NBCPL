LET START() BE {
    WRITEF("Testing FPAIRS liveness tracking bug*N")

    LET vec_a = FPAIRS 4
    LET vec_b = FPAIRS 4

    // Initialize vectors with test data
    vec_a!0 := FPAIR(2.0, 3.0)
    vec_b!0 := FPAIR(1.0, 4.0)

    WRITEF("FPAIRS allocated and initialized*N")
}
