
LET START() BE
$(
    WRITES("Another string.*N")

    LET K=100
    FOR I = 1 TO 20 DO
    $(
       WRITES("In for loop body*N")
	   K := K+1
	   WRITEN(I)
	$)

	WRITEN(K)
    WRITES("end of loop")

    WRITES("*N")

   // FINISH
$)
