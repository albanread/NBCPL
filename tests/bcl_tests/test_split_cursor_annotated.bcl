LET START() BE
$(
   LET S = "This is the age of the train"
   LET L = SPLIT(S," ")

   FOREACH SE AS ^STRING IN L DO
   $(
            WRITES("String: ")
            WRITES(SE)
            WRITES("*N")
   $)
$)
