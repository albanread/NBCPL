LET START() BE {
    LET single_pair = LIST(PAIR(10, 20))

    WRITEF("Testing minimal forEach with single PAIR:*N")

    FOREACH (a, b) IN single_pair DO {
        WRITEF("a=%N, b=%N*N", a, b)
    }

    WRITEF("Done.*N")
}
