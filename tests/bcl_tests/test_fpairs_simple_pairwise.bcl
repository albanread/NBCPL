LET START() BE {
    LET vec_a = FPAIRS 2
    LET vec_b = FPAIRS 2

    vec_a!0 := FPAIR(1.0, 2.0)
    vec_a!1 := FPAIR(3.0, 4.0)

    vec_b!0 := FPAIR(5.0, 6.0)
    vec_b!1 := FPAIR(7.0, 8.0)

    LET result = PAIRWISE_MIN(vec_a, vec_b)

    WRITEF("FPAIRS PAIRWISE_MIN test complete*N")
}
