LET START() BE $(
    WRITEF("Testing basic PAIR addition*N")

    LET p1 = PAIR(10, 20)
    LET p2 = PAIR(5, 15)

    WRITEF1("p1 = %P*N", p1)
    WRITEF1("p2 = %P*N", p2)

    LET p3 = p1 + p2
    WRITEF1("p1 + p2 = %P*N", p3)

    WRITEF("Expected: (15, 35)*N")
    WRITEF("Test complete*N")
$)
